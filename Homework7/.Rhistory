summary(days_init)
summary(days_init)
hospital = read_csv("hospital.csv")
hospital = read_csv("hospital.csv")
hospital = read_csv("hospital.csv")
hospital = read.csv("hospital.csv")
days_add = lm(Days ~ Pressure + Blood + Rate, data = hospital)
days_init = lm(Days ~ Pressure * Blood * Rate, data = hospital)
anova(days_add, days_init)
summary(days_init)
coef(days_init)
coef(days_init)[4]
coef(days_init)[6]
coef(days_init)[7]
coef(days_init)[8]
coef(days_init)
coef(days_init)[4] + 139 * coef(days_init)[6] + 10 *  coef(days_init)[7] + 139 * 10 * coef(days_init)[8]
coef(days_add)
coef(days_add)
coef(days_add)[4]
